# Memory — 2026-03-12

## Ключові події

- **BOT_TOKEN отримано**: `8644972775:AAHT94GCBd-25eFsmEFLp9Ph1JFnCpfJ7i8`
- **Бот @Liquimolli_bot** запущений, PID 10555, `/tmp/bot.log`
- **485 товарів Liqui Moly** імпортовано в PostgreSQL (feed 3411)
- API бекенд працює на `http://localhost:8000`
- PostgreSQL запущено через brew (postgresql@18), юзер `shop`, БД `shop_db`
- SEO генерація запущена в фоні (`/tmp/gen_seo2.py`, `/tmp/seo2.log`), результати в `shop/seo_descriptions.json`
- Маркетинговий план записано в `shop/MARKETING.md`
- docker-compose готовий `shop/docker-compose.yml`
- launchd plist для бота: `~/Library/LaunchAgents/ua.liquishop.bot.plist`
- Системний сон вимкнено: `sudo pmset -a sleep 0`

## Стан проекту

### Готово
- Backend FastAPI (`shop/backend/`)
- Frontend Next.js (`shop/frontend/src/`) — головна, каталог, картка товару
- Telegram бот (`shop/telegram-bot/`) — каталог, кошик, FSM замовлення
- XML імпорт — 485 товарів з наценкою 30%
- SambaNova клієнт (11 ключів + fallback qwen)
- docker-compose + Dockerfiles

### Очікує від Валерія
- Назва магазину (поки "LiquiShop" / "Liquimolli")
- Домен для сайту
- Hetzner VPS аккаунт

### Наступні кроки
- Протестувати бота (Валерій пише /start)
- Endpoint categories/list у бекенді (можливо відсутній)
- Завантажити XML на Prom.ua
- Деплой на Hetzner

## Важливе
- Субагенти без ACP harness не мають доступу до файлів (runtime 0s, 0 tokens) — не використовувати для написання коду
- Між heartbeat-сесіями агент не працює автономно — потрібні cron-скрипти для фонових задач
- `pip3 install X --break-system-packages` — стандартна команда для Python 3.14 на цьому маці
- PostgreSQL PATH: `/opt/homebrew/opt/postgresql@18/bin`

---

## Оновлення 11:55 (2026-03-12)

### Сайт (Next.js)
- App dir знаходиться в `shop/frontend/app/` (не `src/app/` — там застарілий дублікат)
- CartContext (`context/CartContext.tsx`) — localStorage, Provider в layout
- Cart page (`app/cart/page.tsx`) — прізвище/ім'я/по-батькові, телефон +380 з валідацією, success page з посиланням на бот
- Header (`components/Header.tsx`) — лічильник кошика (червоний бейдж)
- ProductCard (`components/ProductCard.tsx`) — кнопка "До кошика" з анімацією
- Сторінки: `app/about/page.tsx`, `app/contacts/page.tsx`
- Dev server PID 15287, `http://localhost:3000`, лог `/tmp/frontend.log`

### Бот (@Liquimolli_bot)
- Новий PID: 15287 (перезапущено після додавання нових хендлерів)
- Лог: `/tmp/bot.log`
- Нові хендлери:
  - `handlers/info.py` — /help, /orders, реєстрація юзерів (register_user / get_all_users)
  - `handlers/favorites.py` — ❤️ обране, /favorites, fav:/unfav: callbacks
  - `handlers/admin.py` — /admin, /broadcast (FSM розсилка), /orders_list
  - `handlers/order.py` — оновлено: валідація телефону, повідомлення адміну після замовлення
- Меню бота оновлено: 6 кнопок (Каталог, Пошук, Кошик, Обране, Контакти, Допомога)
- ❤️ кнопка додана до кожного товару в каталозі

### Автоматизація
- `shop/scripts/sync_prices.py` — скрипт синхронізації цін (запускає import_xml.py для feed 3411)
- Cron кожні 6 годин: `0 */6 * * * python3 sync_prices.py >> /tmp/sync_prices.log 2>&1`
  - УВАГА: crontab команда зависала, можливо не додалась — перевірити при нагоді

### localtunnel
- Встановлено через npx localtunnel для тимчасового публічного доступу
- Пароль = зовнішній IP: `84.30.204.103`
- Тунелі тимчасові, зависають — ngrok потребує реєстрації (ERR_NGROK_4018)

### Відкриті питання
- Prom.ua XML завантаження — ще не зроблено
- Hetzner VPS деплой — очікує рішення Валерія
- crontab sync_prices — перевірити чи додався
- Бот: нові хендлери підключені але не перевірені end-to-end
